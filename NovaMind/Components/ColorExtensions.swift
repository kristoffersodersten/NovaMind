import SwiftUI

// MARK: - Color Extensions
extension Color {
    static let backgroundPrimary = Color(.systemBackground)
    static let foregroundPrimary = Color.primary
    static let foregroundSecondary = Color.secondary
    static let glow = Color.blue
    static let separator = Color.gray
    static let novaBlack = Color.black
    static let novaGray = Color.gray
    static let highlightAction = Color.orange
}
